# 網站維護 SOP

這份文件是給**不懂程式**的你,之後要自己做小幅修改時的操作指南。原則是:文字類的修改你可以自己來,牽動版面/樣式的修改建議還是回來找我。

---

## 🎨 設計規格對照表(給我或未來的你參考)

| 項目 | 數值 |
|---|---|
| 主色 Teal | `#24889E` |
| 淺色 Teal | `#53A6B8` |
| 深色 Teal | `#213E3E` |
| 灰色 | `#AEAEAE` |
| 紅色(錯誤提示) | `#C24644` |
| 標題手寫字體 | Nothing You Could Do |
| 中文字體 | Noto Sans TC |
| 英文/數字字體 | Open Sans |
| 作品卡片尺寸 | 400 × 520px |
| 卡片內圖片尺寸 | 360 × 366px,圓角上 40 / 下 0 |
| 卡片外框圓角 | 上 50 / 下 0 |

---

## ✅ 你可以自己做的事(文字類)

### 修改文字內容
1. 用「記事本」或「TextEdit」打開 `index.html`(不要用 Word!Word 會破壞格式)
2. 用 Ctrl+F(Mac 是 Cmd+F)搜尋你想改的文字,例如搜尋 `Illustrator / Graphic Designer`
3. 直接改文字內容,存檔即可
4. **注意**:只改中英文文字本身,不要動到 `<...>` 尖括號裡的內容(那是程式碼)

### 更新社群連結 / 表單網址
搜尋 `TODO`,依照註解說明把 `#` 換成正確網址即可(詳見 `README.md`)。

---

## ⚠️ 建議回來找我的事(牽動版面/圖片類)

以下這些改動,強烈建議把「你想要的效果」+「新的圖片檔案」直接給我處理,自己動手容易改壞版面:

- 更換作品集圖片(需要重新裁切比例、壓縮檔案大小)
- 新增/刪除作品卡片
- 調整任何顏色、間距、字級
- 新增網站頁面或區塊

## 🔄 更新後怎麼上線

改完 `index.html`(或收到我給你的新檔案)之後:

### 如果用 GitHub Pages:
1. 到你的 GitHub repository 頁面
2. 把改好的檔案重新上傳,蓋掉舊檔案(拖曳上傳即可)
3. 等 1-2 分鐘,網站會自動更新

### 如果用 Netlify:
1. 把整個資料夾重新拖曳到 Netlify 後台上傳區
2. 幾秒鐘後自動更新(不需要等待)

兩種都不需要「重新設定」網站,只是換掉檔案內容而已。

---

## 🆘 什麼時候該回來找我

- 想加新的作品分類(像 Exhibition、Animation 那種新卡片)
- 想調整任何看起來「怪怪的」版面細節
- 想串接新功能(例如線上金流、多國語言、部落格文章)
- 任何你不確定「改了會不會壞掉」的地方

直接把你想要的效果講清楚,或附上截圖/Figma 連結,我可以幫你改好後重新打包給你。
